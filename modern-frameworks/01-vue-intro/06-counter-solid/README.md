# Лічильник на Solid

```bash
npm install && npm run dev
```

Той самий інтерфейс, що й в інших прикладах: signals утворюють залежності,
а JSX прив’язує їх до конкретних DOM-вузлів без повторного виконання компонента.
